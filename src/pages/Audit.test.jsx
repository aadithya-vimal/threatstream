// @vitest-environment jsdom
import React from 'react';import {cleanup,render,screen} from '@testing-library/react';import {afterEach,beforeEach,describe,expect,it,vi} from 'vitest';import {Audit} from './Audit';import {api} from '../lib/api';
vi.mock('../lib/api',()=>({api:{getAuditEvents:vi.fn()}}));vi.mock('../contexts/TenancyContext',()=>({useTenancy:()=>({currentWorkspace:{id:'workspace-1',name:'Product Security'}})}));vi.mock('../layouts/DashboardLayout',()=>({default:({children})=><main>{children}</main>}));
describe('Audit log',()=>{beforeEach(()=>vi.clearAllMocks());afterEach(cleanup);it('shows permission failures safely',async()=>{api.getAuditEvents.mockRejectedValue({status:403,message:'Workspace permission denied',correlationId:'safe-id'});render(<Audit/>);expect(await screen.findByText('Audit access is restricted')).toBeTruthy();expect(screen.getByRole('alert').textContent).toContain('safe-id')});it('renders real audit events',async()=>{api.getAuditEvents.mockResolvedValue([{id:'event-1',action:'team.created',actor_email:'operator@example.test',target_type:'team',target_id:null,result:'success',created_at:'2026-07-19T12:00:00Z'}]);render(<Audit/>);expect(await screen.findByText('team · created')).toBeTruthy();expect(screen.getByText(/operator@example.test/)).toBeTruthy()})});
